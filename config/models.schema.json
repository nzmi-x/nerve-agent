{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nerve.local/models.schema.json",
  "title": "nerve model catalog",
  "description": "Committed catalog of usable models. No secrets — API keys live in .env (GEMINI_API_KEY / DEEPSEEK_API_KEY). Adding a model = adding an entry here, never a code path.",
  "type": "object",
  "required": ["models"],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Path to this schema file, enabling editor IntelliSense + validation."
    },
    "models": {
      "type": "array",
      "description": "Usable models, roughly ordered by increasing capability/cost.",
      "minItems": 1,
      "items": { "$ref": "#/$defs/model" }
    }
  },
  "$defs": {
    "model": {
      "type": "object",
      "required": ["id", "provider"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Exact model id sent to the provider API (e.g. \"deepseek-v4-flash\", \"gemini-3.5-pro\")."
        },
        "provider": {
          "enum": ["gemini", "deepseek"],
          "description": "Which provider serves this model. nerve supports ONLY these two."
        },
        "label": {
          "type": "string",
          "description": "Human-friendly description shown in the picker / status line."
        },
        "default": {
          "type": "boolean",
          "description": "Mark exactly one model as the startup default. If none is marked, the first entry is used."
        },
        "subagent": {
          "type": "boolean",
          "description": "Mark the (cheap) model that subagents run on (the `task` tool, D6). If none is marked, the default is used."
        },
        "temperature": {
          "type": "number",
          "minimum": 0,
          "maximum": 2,
          "description": "Optional sampling temperature override for this model."
        },
        "effort": {
          "enum": ["off", "minimal", "low", "medium", "high", "xhigh"],
          "description": "Default thinking effort (D52). Use a level the provider supports — DeepSeek: off/high/xhigh; Gemini: minimal/low/medium/high (minimal not on Pro). Runtime-overridable via the /model picker and /effort. Unset → falls back to the legacy `thinking` boolean, then off."
        },
        "efforts": {
          "type": "array",
          "items": { "enum": ["off", "minimal", "low", "medium", "high", "xhigh"] },
          "description": "Optional: narrow the SELECTABLE efforts for this model to a subset of the provider's set (e.g. Gemini Pro = [low, medium, high], no minimal). Unset → the provider's full set."
        },
        "thinking": {
          "type": "boolean",
          "description": "Deprecated — use `effort`. Legacy boolean: true → high, false → off."
        },
        "contextWindow": {
          "type": "integer",
          "minimum": 1,
          "description": "Max context window in tokens — drives the 'context used / max' indicator."
        },
        "pricing": {
          "type": "object",
          "additionalProperties": false,
          "description": "USD per 1,000,000 tokens — drives session cost tracking. Omit for an unreleased/unknown model.",
          "required": ["input", "output"],
          "properties": {
            "input": { "type": "number", "minimum": 0, "description": "Input (prompt) price per 1M tokens, USD (standard / cache-miss rate)." },
            "cachedInput": { "type": "number", "minimum": 0, "description": "Optional cache-hit input price per 1M tokens, USD." },
            "output": { "type": "number", "minimum": 0, "description": "Output (completion) price per 1M tokens, USD." }
          }
        }
      }
    }
  }
}
