# Copy to .env (gitignored) and fill in. Bun auto-loads .env — no dotenv.
# These are the ONLY secrets nerve needs. Never put keys in models.toml or any committed file.
GEMINI_API_KEY=
DEEPSEEK_API_KEY=
